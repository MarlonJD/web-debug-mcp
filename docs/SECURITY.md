# Security

`web-debug-mcp` is a local debugging tool that can observe and control a running browser. The default posture is least privilege for local development: explicit target selection, loopback-only URLs, isolated launch mode, bounded evidence, and redaction.

## Trust boundaries

| Boundary | Invariant | Enforcer | Verification | Owner/update trigger |
| --- | --- | --- | --- | --- |
| MCP caller to server | Tool inputs are schema-validated and session IDs are required for runtime actions | Zod schemas in `src/index.ts` and `SessionManager` lookup | `npm run typecheck` and session tests | Platform Engineering; public tool changes |
| Plugin package to local MCP process | The Codex/ChatGPT/Claude Code plugin only launches the existing stdio server; its bundled runtime is pinned to immutable public npm release `web-debug-mcp@0.8.0`, ships built output, requires no install-time script, and does not add browser, filesystem, or network authority. The plugin `.mcp.json` uses only its supported launch fields; strict `required = true` is a direct Codex `config.toml` policy | `plugins/web-debug/.codex-plugin/plugin.json`, `plugins/web-debug/.claude-plugin/plugin.json`, `plugins/web-debug/.mcp.json`, README strict host override, and package binary contract | Plugin validator, packed-package binary handshake, registry metadata, skill binding gate, and `npm run harness:check` | Platform Engineering; plugin distribution or release changes |
| Qualification metadata to native tests | Qualification JSON contains only reviewed provenance, IDs, mappings, verdict inputs, and artifact references; executable actions remain typed repository-native code, candidates cannot self-approve, and the read-only validator stays within an explicit project root | `manual-parity-qualification` skill, artifact contract, and validator | Plugin-skill contract tests and `npm run harness:check` | Platform Engineering; qualification schema or authority changes |
| Server to browser | Remote page/remote CDP targets are rejected unless `allowRemote` is explicit; top-level origin is fixed before first navigation and never rebased after redirects/actions; secondary pages are blocked or quarantined | Core origin policy, selected-target Chromium CDP interception plus context fallback for frame-less documents, Safari window/final-URL checks | Chromium and Safari adapter policy tests plus live smoke | Platform Engineering; target policy changes |
| Browser to evidence | Console, URLs, locals, and evaluated values are bounded; structured keys plus raw authorization/cookie headers, aliases, quoted/escaped values, and multiline values are defensively redacted | `src/core/redaction.ts`, adapter bounds, `composeEvidence` | Redaction corpus, MCP error, Next log, and full deterministic tests | Platform Engineering; new data source or privacy finding |
| Page WebMCP to server | A page-provided `document.modelContext` is untrusted callable API only; discovery uses same-origin top-level registrations, direct calls require explicit side-effect authorization, execute once, validate opaque string/null results, and never infer native browser provenance | `src/adapters/webmcp.ts`, `ChromiumAdapter`, `SessionManager` | WebMCP contract tests and command-owned Chrome smoke | Platform Engineering; page API or direct-action contract changes |
| Local framework/WebDriver transport | Response bodies are counted while streaming before parsing; screenshots use a distinct finite transport cap | `src/core/http.ts`, Next/Vite/WebDriver callers | Bounded HTTP and adapter tests | Platform Engineering; transport or cap changes |
| MCP result boundary | Every tool returns and is checked against its concrete advertised data schema; canonical data, errors, previews, inline images, links, and the complete result are bounded | `src/core/mcp-response.ts`, `src/domain/wire-schemas.ts`, canonical effect table | MCP response/routing tests and native harness | Platform Engineering; public output changes |
| Browser profile and retries | Launch-owned Chromium creates a fresh process/context/profile/storage/cache/service-worker/navigation state per repeated attempt; attached Chromium and Safari retain the exact selected target and disclose profile isolation as unavailable | `ChromiumAdapter`, `SafariAdapter`, and `SessionManager` reset metadata | Adaptive session tests and live smoke summaries | Platform Engineering; browser lifecycle changes |
| Safari WebDriver/BiDi | Safari endpoint is loopback-only by default, browser selection is explicit, BiDi console/network data is bounded and redacted, and unsupported debugger data is not fabricated; Safari network fallback is disclosed | `SafariAdapter` endpoint policy, BiDi subscription, performance fallback, and capability warnings | `safari-adapter.test.ts` and Safari smoke | Platform Engineering; Safari transport changes |
| External Safari MCP diagnostics | A separately configured Safari MCP may create one owned local tab and use only handle-scoped navigation, console summaries, network summaries, and owned-tab close; ambient tools, full request detail, evidence merging, and qualification verdicts are prohibited | `web-debug-workflow` Safari MCP diagnostic reference and external connector availability | Plugin-skill contract and native harness; live use remains environment-specific | Platform Engineering; Safari MCP workflow guidance or vendor schema changes |
| Replay timeline | Captured frames are redacted and bounded; fill/select values are replaced before storage; restore executes only retained non-input actions and fails closed for sanitised inputs | `SessionReplay`, manager restore gate, frame caps, and `redactValue` | Replay/session tests and live React/Vite smoke | Platform Engineering; replay fields or retention changes |
| WebMCP direct-action timeline | Every attempted direct call appends only a non-executable `action: null` frame, permanently marks the generation non-restorable, registers nested strings as session secrets before discovery, and suppresses all later screenshots; `readOnlyHint` never weakens these rules | `SessionManager`, `SessionReplay`, `private-values`, capture projection | WebMCP adversarial tests and smoke | Platform Engineering; direct action safety changes |
| Evaluation | CDP evaluation rejects side effects by default | `web_debug_evaluate` schema and `throwOnSideEffect` | Type check, MCP routing tests, and live Chromium side-effect rejection smoke | Platform Engineering; evaluator behavior changes |
| Artifact storage | Screenshots use a temporary per-session directory and are not written into the project; manual summary creates no pixels, full/include require explicit pixel scope, and auth/private input still suppresses capture. Local paths never enter capture data; quotas and opaque resource identity checks remain enforced | `SessionManager`, `session-evidence.ts`, and `ArtifactStore` | Capture-profile, screenshot, matrix quota, pruning, and artifact identity tests | Platform Engineering; artifact policy changes |
| Closed-session memory | Closing removes the managed session after clearing raw start URLs, auth state, private actions, replay, capture cursors, secrets, endpoints, executable path, and target identity; only 32 sanitized tombstones remain | `SessionManager.closeInternal`, `SessionReplay`, capture cursor state | Session lifecycle/tombstone/cursor tests | Platform Engineering; close contract changes |
| Next runtime metadata and logs | Next MCP calls stay on the selected local origin; log tails resolve inside the detected project root and are byte/line bounded and redacted | `NextAdapter` endpoint derivation, path boundary, caps, and redaction | `next-adapter.test.ts` and live Next smoke | Platform Engineering; Next endpoint or log-field changes |
| Next inspection operations | Only `compile_route` and `get_server_action_by_id` are reachable through schema-bounded `web_next_inspect` requests; browser-observed Server Action execution is recorded as evidence, while arbitrary server-side action invocation remains blocked | `src/index.ts` schema, `SessionManager`, and `NextAdapter` allowlist | MCP contract, adapter tests, and live Next smoke | Platform Engineering; inspection operation changes |
| Framework runtime metadata | Selected React/Angular/Vue bridges are registered only on the selected Chromium target and every CDP script identifier is removed on detach; Angular uses documented globals, Vue safely chains its hook without DOM-private fallback, and Vite middleware exposes bounded local development data | Target-scoped CDP injection, framework adapters, Vite transform cache, in-page/core redaction, and optional pruning | Chromium isolation, bridge contracts, exact fixtures, and framework live smokes | Platform Engineering; bridge, module, or evidence-field changes |
| Recorded scenario contract | Scenarios are session-owned, capped, in-memory, and never persisted across processes; raw fill/select values remain private, contract hashes contain sanitized actions, and caller build labels are untrusted | `SessionManager` private/public scenario split, SHA-256 contract hash, redacted responses | Session and MCP schema tests | Platform Engineering; scenario contract changes |
| Adaptive evidence output | Required URL/DOM/console observations are tri-state with freshness/provenance; only bounded representative evidence is retained and every result is capped at 256 KiB | `SessionManager`, `composeEvidence`, redaction/bounds | Deterministic verification tests and harness gate | Platform Engineering; evidence-shape changes |
| Elevated browser modes | TLS bypass and auth seeding require isolated Chromium launch, one exact approved loopback origin, request/WebSocket/page guards, and blocked service workers; Safari rejects both capabilities | `SessionManager`, `ChromiumAdapter`, `SafariAdapter` | TLS/auth/escape smoke and adapter tests | Platform Engineering; target-policy changes |
| Disposable auth state | Storage state is read through one contained no-follow descriptor, bounded and scope/lifetime validated, and registered only as redaction secrets; auth-seeded screenshots are suppressed because pixels cannot be truthfully redacted | `src/core/auth-state.ts`, `SessionManager`, `ChromiumAdapter` | Auth validation/race/redaction tests and local-fidelity smoke | Platform Engineering; credential/evidence changes |
| Process cleanup authority | Cleanup and startup reconciliation may delete only an owner-only exact registry artifact family whose PID is dead or whose executable/package/start identity no longer matches. Startup never signals; cleanup may signal only a live matching idle record after lock and state revalidation. Active, live-matching, identity-unverifiable, malformed, and unknown entries remain protected; unregistered processes are out of scope | `src/core/process-registry.ts`, `src/index.ts`, `bin/web-debug-mcp.mjs` | Lifecycle/accounting/startup-reconciliation/PID-reuse/sidecar tests and bounded CLI report | Platform Engineering; process lifecycle changes |
| Current-task MCP binding | A package/registry readiness check cannot authorize a Codex task's tool namespace; only a real bundled `web_project_detect` call proves Gate 0 | `web-debug-workflow` skill, doctor `client-binding` check, MCP server instructions | Plugin-skill/doctor contract tests and explicit startup diagnostics | Platform Engineering; host/plugin binding or recovery guidance changes |

## Sensitive data policy

The core adapter does not collect cookies, authorization headers, browser storage, or raw response bodies by default. It does collect URLs, console text, debugger locals, DOM text, and screenshot pixels because those are required for debugging; each is bounded and redacted before returning to the MCP caller. Callers must use disposable local credentials and synthetic data for live sessions.

The qualification skill adds no credential, browser, network, database, or release authority. Qualification catalogs, crosswalks, and run records must not contain credentials, cookies, tokens, storage state, authorization headers, private fill values, request bodies, SQL, or executable expressions. Native test artifacts follow the target repository's access and retention policy; a local digest detects drift but is not an attestation.

The Next adapter reads the development server’s MCP metadata surface over the already selected local origin. It does not authenticate to a remote Next server or execute arbitrary Next runtime tools; only the allowlisted metadata tools are called. When `get_logs` returns a path, the adapter resolves it, requires the file to stay inside the detected project root and retain the expected Next development-log basename, then reads only a bounded tail and redacts it. The Vite plugin similarly exposes only bounded module metadata on the local dev server and is not intended for production.

The redaction layer is defensive, not a guarantee of secrecy. New adapters must document every new field and add a regression test before exposing it through `EvidenceBundle`. Auth-seeded sessions intentionally create no screenshot file or resource handle; structured text and metadata remain defensively redacted. Fill and select values are private live-session inputs and public replay replaces both with a marker.

Angular and Vue page-runtime bridges are development-only, target-scoped, read-only observers selected from private project metadata. They serialize bounded data descriptors, redact sensitive keys in-page, and are redacted again by core. Angular does not traverse private Ivy arrays or invoke accessors, methods, signals, injectors, or profiling. Vue chains only the pinned DevTools hook contract and never reads DOM-private `__vue*` properties. Raw component/app instances remain inside the page; checks-only and private-input scenario frames omit runtime bundles.

Chromium attach mode installs CDP `Fetch` interception only on the selected target, but popup-first requests can arrive before Playwright exposes a frame. The fail-closed fallback therefore uses a `BrowserContext` route for the lifetime of the session: framed requests from sibling pages and frame-less non-document requests fall through, while every frame-less document request is aborted. Playwright disables HTTP cache while that context route is installed, so attached sibling pages can observe changed cache behavior even though their DOM and React runtime are not injected or navigated. Close the session to remove the route and restore normal context behavior.

Recorded scenarios retain raw fill/select values only in the live session's private memory. Public scenarios, contract hashes, attempt summaries, build references, errors, replay frames, screenshots, and representative evidence scrub action values before serialization, including values appearing in dynamic DOM ids, nested framework data, details, and artifact handles. Screenshot files remain under the owning session's temporary directory; if redaction would collide with a path, only the serialized handle is set to null and the original file is not copied or moved. The executable URL is private and the public scenario URL removes its query. The contract hash is an ordinary SHA-256 of the sanitized contract; it is not an authenticated build identity and cannot support cross-session reuse. Closing purges private scenarios, retained evidence, auth state, raw start settings, secrets, and target identity before removing the managed session.

## Abuse and recovery

The server binds to MCP stdio and the browser adapter uses explicit local targets. Do not expose a CDP endpoint or a future daemon on a non-loopback interface without a separate authentication and threat-model decision. If a session is attached to the wrong browser, close it immediately, discard the evidence, and restart with launch mode and an isolated profile.

Repository-local boundary review and deterministic regression tests cover origin navigation, auth-state containment, redaction, response budgets, artifact identity, session teardown, and process ownership. No external penetration test or production hardening claim has been performed; this remains a local development tool.
