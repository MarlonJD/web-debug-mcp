# Verification Matrix

| Change surface | Fast check | Broader check | Behavioral evidence | Fallback or blocker | Owner/update trigger |
| --- | --- | --- | --- | --- | --- |
| Core/session logic | `npm test -- --run test/session-manager.test.ts` | `npm test` | Session starts, records a reproduced baseline, verifies adaptive outcomes, enforces leases/deadlines, bounds evidence, evaluates live locator probes, checkpoints, and matrices without replacing canonical state | Live browser is not required for pure/fake-adapter contract tests | Platform Engineering; lifecycle change |
| Redaction/privacy | `npm test -- --run test/redaction.test.ts test/mcp-response.test.ts test/next-adapter.test.ts` | `npm test` | Sensitive structured keys, raw headers, aliases, quoted/escaped values, and URLs are redacted while ordinary prose, IDs, sibling fields, and line boundaries remain | Add a regression test for every new exposed field or representation | Platform Engineering; evidence-shape change |
| MCP schemas and routing | `npm test -- --run test/mcp-server.test.ts test/mcp-routing.test.ts` | `npm run typecheck && npm run build` | All 13 tools advertise output schemas/effect annotations and forward normalized inputs/context through in-memory MCP | Live stdio handshake remains a distribution check | Platform Engineering; public tool change |
| Structured output/artifacts | `npm test -- --run test/mcp-response.test.ts test/http.test.ts test/mcp-artifact.test.ts` | `npm test` | Canonical structured envelope, total result cap, bounded readers, inline/resource screenshot policy, containment, identity drift, quota pruning, and stable early expiry | Oversized output returns a stable error | Platform Engineering; output or artifact change |
| Doctor CLI | `npm test -- --run test/doctor.test.ts` | `npm run build` then the built `doctor` command | Bounded JSON reports Node/project/browser/target/framework readiness without launching an arbitrary browser | Browser/URL checks are literal pass/warn/fail/skipped | Platform Engineering; setup contract change |
| MCP distribution | `npm pack --dry-run --json` | Install the exact final `0.6.0` archive in clean temporary prefixes and start its binary under Node 20/22/24 | Archive contains `bin/web-debug-mcp.mjs`, built `dist`, dependencies, README, and a 13-tool concrete-schema stdio handshake | Public-registry verification uses an empty prefix and cache after publication | Platform Engineering; package metadata or install command change |
| Plugin distribution | Plugin validator plus `npm run harness:check` | Install `web-debug` from the repository marketplace and handshake its immutable npm runtime | Both client manifests discover the same `.mcp.json` and two separately routed skills; bundled MCP stays pinned to public `web-debug-mcp@0.6.0` | First uncached launch needs Node.js 20+, npm, and network access to the published package | Platform Engineering; plugin metadata, skill, or MCP launch path change |
| Manual-parity qualification | `npm test -- --run test/plugin-skill-contract.test.ts` | Skill/plugin validators plus approved and adversarial metadata fixtures | Approved/full/clean native evidence passes; candidate self-approval, JSON action fields, crosswalk gaps, API-only UI claims, ambiguous mutation, missing facets, and aggregate drift fail | Structural validation does not authenticate reviewers, artifacts, or product behavior | Platform Engineering; qualification schema or routing change |
| Browser adapter | `npm run typecheck` | `npm run smoke:live` | DOM, console, network, screenshot, debugger, and live side-effect-rejection evidence from a loopback page | `WEB_DEBUG_CHROME_EXECUTABLE_PATH` or CDP endpoint may be unavailable | Platform Engineering; Playwright/CDP change |
| Origin/remote policy | `npm test -- --run test/chromium-policy.test.ts test/safari-adapter.test.ts test/redirect-policy.test.ts` | Approved remote CDP target smoke | Default-deny endpoint policy, fixed top-level origin, zero-hop framework/doctor redirect policy, redirect/action/popup rejection, subresource allowance, Safari quarantine, and non-isolated metadata | Safari interception is post-navigation; no approved external remote host is available | Platform Engineering; target policy change |
| Fixture behavior | `npm test -- --run test/fixture-contract.test.ts` | Fixture server + live browser flow | Submit action produces the declared status text | No browser means structural fixture proof only | Test ownership; fixture change |
| Comparison demo | `node --check scripts/demo-compare.mjs` | `npm run demo:compare -- --runs=1` | Vanilla, React/Vite, Next, logic repair, async repair, and visual repair flows report baseline/MCP timings and evidence coverage | Requires an explicit Chromium executable; human diagnosis time is not inferred from machine timings | Platform Engineering; demo scenario or metric change |
| Agent task evaluation | `npm test -- --run test/eval-contract.test.ts` | `npm run eval:catalog`, `npm run eval:grade -- <result.json>`, plus isolated manual agent arms | Three frozen prompts expose deterministic behavioral graders and required provenance/usage fields without calling models; reviewed records receive machine pass/fail | Root-cause quality and model runs remain explicitly reviewed/manual | Evaluation ownership; task or grader change |
| Compatibility | `npm test -- --run test/compatibility.test.ts` | Exact React/Vite/Next/Chromium/Safari live smokes | Root and fixture versions stay aligned; docs distinguish declared, verified locally, and candidate | Broader Node/framework/browser matrices remain candidate until run | Platform Engineering; dependency or runtime claim change |
| Repair demos | `node --check scripts/demo-compare.mjs` | `npm run demo:compare -- --scenario=complex-logic-fix --runs=1` and `--scenario=visual-layout-fix --runs=1` | Buggy state reproduces, MCP evidence links the runtime symptom, fixed flow passes, visual geometry holds at desktop/mobile sizes, and semantic failures exit non-zero | Runs use temporary fixture copies; screenshot pixels remain browser/font dependent | Platform Engineering; repair fixture or layout invariant change |
| Explicit viewport | `npm test -- --run test/session-manager.test.ts` | `npm run demo:compare -- --scenario=visual-layout-fix --runs=1` | Bounded viewport propagates to isolated Chromium and evidence reports the selected dimensions | Attached browser profiles may not honor viewport overrides; visual demo uses launch mode | Platform Engineering; browser start contract change |
| React/Vite adapter | `npm run typecheck` | `npm run smoke:react-vite` | Component tree, hook state, bounded commit profiler/flat flamegraph/inferred render cause, source breakpoint, Vite module graph/HMR status and transform diff, screenshot, and scenario verification | Requires Vite dependencies and an explicit Chromium executable | Platform Engineering; React bridge, Vite plugin, or fixture change |
| Vue/Vite adapter | `npm test -- --run test/vue-adapter.test.ts test/vue-fixture-contract.test.ts` | `npm run smoke:vue-vite` | Safely chained Vue 3 app/component tree, props/state changed keys, update counts, source hints, Vite graph, and HMR diff | Exact Vue 3.5.42 hook contract only; no DOM-private fallback or Safari enrichment | Platform Engineering; Vue hook, bridge, or fixture change |
| Angular adapter | `npm test -- --run test/angular-adapter.test.ts test/angular-fixture-contract.test.ts` | `npm run smoke:angular` | Angular 21 documented-global DOM-host tree, own state, changed keys, private-Ivy omission, and generic browser evidence | Development builds only; Angular CLI Vite is not the Web Debug endpoint | Platform Engineering; Angular bridge, CLI, or fixture change |
| Next adapter | `npm run typecheck` | `npm run smoke:next` | Next runtime tools, routes, project metadata, compilation issues, bounded server log tail, request insights, normalized request traces, Server Action request/resolution/linkage evidence, route handler, and browser evidence | Requires Next dev server and an explicit Chromium executable | Platform Engineering; Next adapter or fixture change |
| Next inspection | `npm run typecheck` | `npm run smoke:next` | Allowlisted route compilation and Server Action resolution through the single MCP facade | Requires Next 16 development MCP endpoint; missing optional tool remains explicit | Platform Engineering; `web_next_inspect` schema or adapter change |
| Safari adapter | `npm run typecheck` | `npm run smoke:safari` | WebDriver actions, DOM, screenshot, BiDi console, network event/fallback evidence, and explicit debugger capability warning | New hosts must enable Safari remote automation; unavailable BiDi console is a literal blocker for `noConsoleErrors`; Safari 27’s official MCP is an external alternative, while this adapter remains the single-facade fallback | Platform Engineering; Safari adapter or browser-selection change |
| Replay timeline | `npm test` | `npm run smoke:react-vite` | Bounded representative frames, sanitised form actions, frame seek, and safe restore | Older frames may be truncated after the eight-frame cap; sanitised inputs cannot be restored | Platform Engineering; replay/session lifecycle change |
| Harness/docs | `npm run harness:check` | `python3 /Users/marlonjd/.codex/skills/harness-engineering/scripts/harness.py check --root .` | Required routes, commands, docs, and tool names remain discoverable | Harness skill path is external tooling; project check remains native | Platform Engineering; authority or command change |
| Security boundary | Focused origin/redaction/HTTP/artifact/session tests | Manual local review of `docs/SECURITY.md` plus live redaction/evaluation exercise | Origin rejection/quarantine, bounded transports/results, secret purge, artifact identity, and redaction evidence | External penetration testing and production hardening remain unavailable | Platform Engineering; trust-boundary change |
| Smoke process lifecycle | `npm test -- --run test/managed-process.test.ts` | Relevant live smoke | Readiness timeout, early-exit failure, awaited SIGTERM, and exact command-owned process-group SIGKILL escalation including stubborn grandchildren | GUI/browser cleanup remains proportional to the invoked smoke | Platform Engineering; smoke lifecycle change |
| Local fidelity | `npm run smoke:local-fidelity` | Real TLS/auth/accessibility/checkpoint/matrix fixture flow | Strict self-signed TLS fails, guarded opt-in succeeds, auth screenshot is suppressed, AX and named checkpoint evidence are present, and matrix entries are bounded | Requires explicit Chromium executable; unsupported Safari advanced surfaces remain unavailable | Platform Engineering; elevated mode or matrix change |
| Process lifecycle | `npm run typecheck && node bin/web-debug-mcp.mjs cleanup` | Command-owned child/TTL/EOF and MCP routing/accounting tests | Owner-only registry, absolute active-session reconciliation, request accounting, repeated/concurrent close idempotency, bounded cleanup report, and unregistered-process survival | Identity-unavailable/unsupported platforms fail closed | Platform Engineering; lifecycle change |

Use the narrowest fast check first. A missing browser blocks only live browser evidence; it does not turn deterministic core tests into a pass by inference.
