# Agent Capability Registry

| Capability | Entry point | Purpose | Expected signal | Owner/update trigger | Status |
| --- | --- | --- | --- | --- | --- |
| Repository setup | `npm install --no-audit --no-fund` | Install pinned dependencies | Exit 0 and current lockfile | Platform Engineering; dependency change | verified locally |
| Focused tests | `npm test` | Exercise redaction, detection, sessions, and fixture contracts | Vitest exits 0 | Platform Engineering; behavior change | verified locally |
| Type/build validation | `npm run typecheck && npm run build` | Type-check source and tests, then emit the server | Both commands exit 0 | Platform Engineering; source/dependency change | verified locally |
| Project-native harness gate | `npm run harness:check` | Check required paths, public tools, docs, and stdout boundary | Prints `harness-check: PASS` | Platform Engineering; repository contract change | verified locally |
| Before/after comparison demo | `npm run demo:compare` | Compare a raw Playwright/source baseline with structured MCP evidence across vanilla, React/Vite, and Next fixtures | Markdown or JSON report with median/p90 timings and added evidence fields | Requires an explicit Chromium executable; machine timings do not represent human diagnosis time | Platform Engineering; demo scenario or metric change | verified locally |
| Agent task contracts | `npm run eval:catalog` / `npm run eval:grade -- <result.json>` | Emit three frozen repair prompts without calling a model and deterministically score bounded reviewed records | Bounded catalog plus pass/fail grading JSON | Agent arms and root-cause review are manual/isolated and grading input is a trusted record | Evaluation ownership; task or grader change | verified locally |
| Doctor readiness | Built `web-debug-mcp doctor ...` | Check exact Node/project/browser/URL/framework readiness without launching arbitrary browser state | One bounded JSON report and exit `0`, `1`, or `2` | Live URL and browser configuration are caller supplied | Platform Engineering; setup contract change | verified locally |
| Complex repair demos | `npm run demo:compare -- --scenario=complex-logic-fix` / `visual-layout-fix` | Reproduce a stale derived-state bug or responsive drawer bug, apply an exact temporary fix, and verify the same flow | Buggy reproduction, root-cause evidence, fixed verification, and desktop/mobile geometry/screenshot evidence | Temporary fixture copies are used; screenshot pixels are environment-dependent | Platform Engineering; repair scenario or invariant change | verified locally |
| Model QA arms | `gpt-5.6-sol + xhigh` / `gpt-5.6-luna + max` | Run the same repair QA prompt in separate agent environments | Both arms pass core gates and repair contracts; report model wall time separately from browser timing | A port collision or concurrent workspace mutation invalidates a frozen-snapshot comparison | Evaluation ownership; prompt, model, or isolation change | verified locally |
| Packaged MCP distribution | `npm pack --json --pack-destination <owned-dir>` | Install the exact `0.4.0` tarball into a fresh prefix and exercise stdio plus package-only commands | `web-debug-mcp-0.4.0.tgz` installed 96 packages; fresh-prefix stdio reported version `0.4.0`, 13 tools, output schemas, bounded help/doctor/isolated-cleanup, and clean close | Public npm verification follows immutable publication from an empty directory | Platform Engineering; package metadata or install command change | verified locally — shasum `c7daee55f175d113503d4e662ea8bc418da149ea` |
| Codex/ChatGPT/Claude Code plugin distribution | `npm run harness:check`; `npm view web-debug-mcp@0.4.0`; `codex plugin marketplace upgrade web-debug`; `codex plugin add web-debug@web-debug` | Validate public npm package, manifests/config/skill, and install the released Codex plugin | npm, fresh-prefix stdio, bundled runtime, and installed plugin must agree on `0.4.0`; Codex build identity is `0.4.0+codex.20260829203143` | Claude live reload is unavailable on this host and remains static-manifest validation | Platform Engineering; plugin metadata or install path change | candidate — publication and install pending |
| Fixture runtime | `npm run serve:fixture` | Launch deterministic loopback browser target | Reports `http://127.0.0.1:4173/` | Test ownership; fixture behavior change | candidate |
| Live browser smoke | `npm run smoke:live` | Exercise CDP breakpoint, pause-safe capture, screenshot, and cleanup | JSON reports `passed: true` | Platform Engineering; Chromium/CDP change | verified locally |
| Remote CDP policy | `test/chromium-policy.test.ts` | Verify remote endpoint default-deny, protocol validation, and explicit non-isolated attach contract | Deterministic `REMOTE_CDP_BLOCKED`/protocol failures before connection; positive external evidence requires an approved host | Platform Engineering; remote target policy change | verified locally |
| Live React/Vite smoke | `npm run smoke:react-vite` | Exercise injected React component/state bridge, commit profiler/flat flamegraph/render-cause evidence, Vite source URL/module graph/transform diff, breakpoint, verification, and cleanup | JSON reports `passed: true` with React, profiler, flamegraph, Vite diff, and breakpoint assertions | Platform Engineering; React/Vite adapter change | verified locally |
| Live Next smoke | `npm run smoke:next` | Exercise Next `/_next/mcp`, App Router routes, route handler, bounded server log tail, request insights, normalized request traces, Server Action request/resolution/linkage evidence, browser state, and cleanup | JSON reports `passed: true` with Next runtime assertions | Platform Engineering; Next adapter or fixture change | verified locally |
| Live Safari smoke | `npm run smoke:safari` | Exercise Safari WebDriver actions, DOM, screenshot, BiDi console evidence, network event/fallback evidence, cleanup, and explicit debugger warning | JSON reports `passed: true` with target, console, network source, and debugger-boundary assertions, or reports the exact unavailable-BiDi blocker | Platform Engineering; Safari adapter or browser policy change | verified locally — current run passed with BiDi console evidence, Performance Resource Timing network fallback disclosure, and explicit debugger/profile warnings; prior unavailable-BiDi evidence remains recorded in the active ExecPlan |
| Next inspection | `web_next_inspect` | Compile one local route or resolve one Server Action through the allowlisted Next MCP tools | Structured result with `routeSpecifier`/`issues` or action filename/function | Requires a running Next dev server and a known action ID for action lookup | Platform Engineering; Next inspection contract change | verified locally |
| Replay seek/restore | `web_replay_seek` | Inspect one retained redacted frame or restore it through safe retained actions | Structured frame with retained index, trigger, DOM, framework, debugger state, and `restored` result | Platform Engineering; replay timeline change | verified locally |
| MCP runtime | `npm run dev`, `node dist/index.js`, or the packaged `web-debug-mcp` binary | Serve the MCP tool surface over stdio | Process stays alive with protocol stdout only | Platform Engineering; MCP API or package entrypoint change | verified locally |
| Browser evidence | `web_session_start` → `web_issue_capture` | Collect bounded runtime evidence | MCP structured envelope with redaction marker and optional screenshot resource | Platform Engineering; adapter change | candidate |
| Repro verification | `web_repro_record` → `web_fix_verify` | Execute a bounded pre-fix baseline, then verify acceptance/regression checks with adaptive retries | `outcome: verified|failed|inconclusive`, level, rates, attempt summaries, and representative evidence | Platform Engineering; scenario contract change | verified locally |
| Local fidelity flow | `npm run smoke:local-fidelity` | Exercise strict/opt-in loopback TLS, disposable auth, computed AX, emitted locators, checkpoints, and matrix verification | JSON reports `passed: true` for origin guards, screenshot suppression, accessibility, locator round trip, mobile-only reproduction, fixed verification, and both representative evidence phases | Requires local Chromium executable and opens only command-owned HTTPS fixtures | Platform Engineering; elevated mode or matrix change | verified locally |
| Process lifecycle and cleanup | `node bin/web-debug-mcp.mjs cleanup [--all-idle]` | Inspect owner-only registry records and clean only identity-verified idle instances | Bounded stdout JSON report; unrelated/unregistered processes survive | Unsupported platforms and identity drift are reported/skipped, never signaled | Platform Engineering; registry/lifecycle change | verified locally |
| Source-control context | `git status --short --branch` | Inspect local changes before handoff | Current branch and worktree state | Platform Engineering; every handoff | verified locally |

Statuses are literal: `verified locally` means the command ran in this task environment; `candidate` means the path is implemented or documented but has not had the relevant live exercise in this environment. Remote policy is locally verified, but an approved external remote target remains unverified.
