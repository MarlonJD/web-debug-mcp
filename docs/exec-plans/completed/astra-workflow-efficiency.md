<!-- harness-plan:v1
id: astra-workflow-efficiency
status: completed
created: 2026-09-08
updated: 2026-09-08
completed: 2026-09-08
owner: Platform Engineering
-->

# Improve skill decisions and evidence efficiency for Astra

Maintain this plan according to [`../../PLANS.md`](../../PLANS.md).

## Purpose / Big Picture

Keep the model-independent MCP runtime while making its three skills respect task scope and existing authorization. Collect only the evidence needed by a capture, reduce redundant catalog schemas, and measure skill decisions with observable evaluation records. Preserve origin, privacy, cancellation, native-runner, and truthful-verdict contracts.

## Progress

- [x] Established the unchanged baseline: 32 files/169 tests, build, typecheck, and native harness 620 checks passed; certification remains stale-candidate. The local 13-tool catalog is 332,821 JSON bytes (71,936 input schema bytes, 255,398 output schema bytes).
- [x] Revise the three skills and relevant guidance; add one bounded product WebMCP example.
- [x] Add behavioral evaluation cases, comparable experiment records, and independent forward evaluations; the earlier high cohort is complete with all 64 declared cells accounted for (58 strict passes).
- [x] Implement selective capture with truthful uncollected/unavailable/stale state and cursor semantics.
- [x] Reduce catalog schema repetition using supported dependency capabilities and verify client/schema behavior.
- [x] Run focused and required checks, relevant live evidence, process cleanup, and an independent final review. R1 passed 195 tests, typecheck, build, and 621 harness checks; the new real-Chrome regression and React/Vite passed with no owned processes left.
- [x] Compare final baseline/revised skills on Astra max and ultra, then Luna max. Original cohorts, capacity recovery, protected follow-ups, and independent source/claim/ownership reviews are complete. Fresh Luna began only after both protected Astra operational closures.
- [x] Complete the 1,800-second budget amendment: 20 paired Astra follow-up cells, 20 fresh Luna browser/authority cells, and three separate candidate probes completed. Original short/censored and interrupted cohorts remain separate; Luna routing retains its uncensored original guard. Candidate semantic defects and one unsupported Luna environment claim remain explicit.
- [x] Resolve evaluated-command containment after a confirmed out-of-cell process signal, audit prior browser cleanup commands, and resume only with native-verified process/filesystem boundaries and explicit new-cohort provenance. Scoped native probes passed; the 53-stream audit found one explicit signal violation. Protected authoring attests its effective policy, and the browser preflight verified restricted commands alongside an outside-sandbox trusted MCP startup.

## Surprises & Discoveries

- Capture profiles currently select output after a full collection. Only screenshot selection reaches the collector.
- The current Codex task exposes no bundled Web Debug tools. This does not prevent source implementation or native package tests; it does prevent claiming a live current-task Web Debug binding. Model experiments must record actual tool availability rather than fabricate tool use.
- Existing skill checks assert literal wording. Preserve structural and runtime safety tests, but evaluate routing through recorded agent decisions.

- The installed Codex CLI 0.146.0 could not open Astra. An exact task-owned temporary Codex CLI 0.153.4 successfully ran `gpt-6-astra` with `high` reasoning; no personal configuration or installation changed.
- Two routing source directories and their frozen source were externally emptied during early probes. These runs were excluded as contaminated infrastructure evidence and a full fresh cohort was run in a separate sibling directory with integrity guards.
- Initial app-server probes inherited unrelated MCPs. Explicit supported per-thread config disabled unrelated servers/plugins/host skills before the authority comparison; contaminated probes remain separate.
- The first Next smoke hit the existing evidence-size pruner, which correctly removed optional project metadata after a large request trace. The smoke now reads the selected Next metadata before collecting the large action trace; the initial failure is retained.

- Independent review found Safari network collection could be labeled fresh after a failed fallback. Structured network observations now disclose unavailable/stale data, and a fallback without BiDi refreshes on each capture.
- Payment example review found BFCache restore lost registration. Pagehide now aborts the current lifetime and persisted pageshow remounts one fresh registration, with retained callback tests.
- Actual Astra traces exposed short numeric fills corrupting host UUIDs and clocks through whole-envelope redaction. Host-owned session identities and capture/replay/WebMCP clocks now stay intact while page echoes and screenshots remain protected.
- One earlier revised-skill filter run guessed the record schema 15 times. The failure signature now uses a discriminated schema; the final workflow includes a flat valid example and an explicit stopping rule for already-verified checks.

- The original high drawer prompt only asked to fix clipping, while its oracle required full-viewport coverage including the header. Keep those original results and disclose the ambiguity. Before new max/ultra/Luna calls, make that acceptance target explicit in both arms; do not label a reasonable partial interpretation dishonest.
- Final routing cohorts completed at max 11/12 and ultra 12/12. The missing max pass emitted no model work before its deadline; provider waiting versus internal work is unknown. No retry or invented usage was used.
- Max authoring had four implementation-turn timeouts. One revised patch passed all ten independent modern API checks and 12 native tests before timeout, but did not return a final answer. Separate artifact correctness from completed-run success.
- Revised skill examples overlap the Refund/source-review tasks and appeared in broad searches. These are workflow regression checks, not held-out quality evidence. Installed dependencies are shared through fixed symlinks; package locks and frozen files are checked, but dependency contents are not sealed.
- Final independent review reproduced a private `stale` input corrupting adapter-owned observation enums and falsely promoting cached DOM to fresh. The R1 correction preserves only the enum metadata while page strings stay redacted. Five collision regressions failed before the fix and passed afterward; a real Chrome pause confirmed stale evidence, private text redaction, screenshot suppression, and valid output together.
- All short-budget modern authoring implementation turns, including the first Luna pair, were censored at 300 seconds. Several had already produced correct code and passing native tests. The user correctly identified that these bounds materially affect the quality conclusion. Inspect the runner's raw completion/request handling independently before the longer follow-up; do not assume every silence is productive reasoning or provider capacity.
- The timeout-driver audit found no evidenced missed completion, stale turn identity, withheld host reply, or stderr backpressure; late receipts at termination were not fully observable. New long drivers add receipt/drain logging.
- Short Luna original-skill race repetition 1 sent SIGTERM to a pre-existing MCP Chrome PID outside its cell, then claimed it was owned. Product checks passed but operational ownership/claim accuracy failed. All new model launches were held, two fresh authoring inspections were stopped through exact owned groups, and raw evidence was preserved. Actual user/task ownership of the affected Chrome remains unknown; do not relaunch or modify that unrelated session blindly.
- The first protected browser model cohort connected to MCP but could not start sessions: CLI `never` approval policy rejected the state-changing tool call. A startup/catalog preflight does not prove tool invocation authorization. Preserve actual source-only attempts as infrastructure-limited evidence and require an owned no-model browser lifecycle check of narrowly configured Web Debug tool approvals before a fresh comparison.
- The protected Astra follow-ups completed every affected task in both arms. All eight modern authoring implementation turns took longer than the old 300-second guard. Correctness tied; Ultra revised authoring and drawer times improved in both repetitions, while Max times were mixed. These are descriptive results on known tasks, with amended containment/exposure as well as time.
- Ultra's separate candidate preparation was structurally valid and accurately reported, but independent review found un-cloned expected snapshots that can conceal shared-reference corruption. Exact drafted statements passed a faulty API in an isolated native counterexample. Preserve the evaluated artifact and declared grade; report the semantic defect separately and track follow-up review guidance.

## Decision Log

- 2026-09-08: The user authorized all review recommendations and parallel work. Subagents perform read-only research and isolated verification; only the main agent edits this working tree. No branch, publication, installation, or personal configuration changes are included.
- 2026-09-08: The user additionally requested Astra `max` and `ultra`, followed by Luna `max`. Compare baseline/revised skills on the same final frozen runtime, task fixtures, and declared limits, with two repetitions per task. Run Astra settings before the Luna phase; do not conflate old high-cohort fixtures/prompts with the strengthened authority oracle.
- 2026-09-08: Keep the 13-tool facade, native regression ownership, and concrete runtime output validation. API/model selection belongs to evaluation clients, not the MCP core.
- 2026-09-08: Real agent evaluation results must distinguish bounded forward tests from end-to-end browser repair and statistical benchmarks. Missing binding or model access is recorded literally.
- 2026-09-08: After an explicit `serverOverloaded` rejection in an Ultra ambiguity cell, the user requested retries for capacity-full errors. Preserve the original experiment/grade and all attempts; run separately recorded capacity-only retries with identical frozen inputs and short backoff. Do not treat ordinary silent deadlines or failed oracles as capacity failures. Luna remains after the Astra phase, including this recovery.
- 2026-09-08: Preserve all frozen R0 model comparison inputs after discovering the observation-enum bug. Apply the fix to the working tree as R1 and validate it separately; do not silently change later model arms. The revised skill files are identical in R0 and R1.
- 2026-09-08: The user challenged the short deadlines and requested substantially longer or unlimited runs. Raise the infrastructure guard to 1,800 seconds per actual turn/cell and rerun both arms/two repetitions of affected tasks as separate follow-ups. Keep original cohorts and their completion grades, but do not equate timeout censoring with incorrect code or poor final-output quality. Browser prompts explicitly contain the budget, so the follow-up changes that declared line as well as the runner guard; all other task content, R0 runtime, skills, and oracles stay fixed. Fresh Luna cohorts must not silently mix original short-budget prompts with longer ones. Hold unstarted pilot cells; let active attempts finish and clean up.
- 2026-09-08: After the confirmed process-ownership violation, hold model launches until evaluated shell commands have enforced per-cell process/filesystem containment. Keep MCP/browser execution outside the model command sandbox. Apply the same new boundary and explicit ownership guidance to both arms, record effective permission metadata, and disclose that follow-ups change containment as well as time. Native tests target only controller-owned harmless processes/files. Audit existing browser signal commands without touching unrelated processes.

- 2026-09-08: Complete the protected long-budget cohorts without removing the finite infrastructure guard. All twelve Astra/Luna authoring implementation turns exceeded the original 300-second bound and completed correctly. Preserve the original repair/false-success rubric, while separately reporting unsupported failure inferences, citation errors, semantic test defects, and additive review-projection corrections. Track follow-up geometry and candidate-assertion work in DEBT-006/007 instead of changing evaluated skill bytes.

## Outcomes & Retrospective

Implementation, requested model comparisons, and independent reviews are complete locally. Catalog JSON shrank from 332,821 to 124,179 bytes (62.7%) while preserving 13 concrete input/output schemas. This is a byte reduction, not a token, billing, or latency claim. R1 passed 195 tests, typecheck, build, and native harness checks; its enum correction is separate from the frozen R0 model comparisons, whose skill bytes remain identical.

All 20 paired Astra follow-up cells passed the declared rubric. Fresh Luna passed 8/8 authority and 12/12 browser repair cells, with 11/12 browser final answers fully supported: a revised drawer answer inferred a server outage from a network-denied shell probe. Original uncensored Luna routing remains a separate 12/12 cohort. Correctness tied; Ultra revised authoring/drawer times improved on both repetitions, Max time was mixed, and Luna browser schema errors fell 15 to 1 while exported calls rose 179 to 196. No general revised-skill quality superiority is established from two repetitions on known tasks.

All three candidate preparations completed without self-approval. Max's skeletons remained skipped; Ultra has a demonstrated snapshot-alias assertion flaw; Luna ran seven non-gating tests but has unsupported source/coverage claims, assertion gaps, and broken final links. Exact isolated counterexamples and an additive review-field correction preserve the original outputs and grades. Follow-up work is recorded in the technical debt tracker.

The process audit covers 77 browser streams and 508 native commands. It retains the one historical short-Luna cell-unowned Chrome signal and finds no new explicit signals in the added 24 streams. The affected browser's broader owner remains unknown. Protected execution used native-verified command containment and scoped MCP authorization; all evaluated owned processes are reaped. Safari fresh live evidence remains blocked and release/certification authority remains separate.

## Context and Orientation

Skills live in `plugins/web-debug/skills`. `src/index.ts` owns the MCP facade. Capture collection is in `src/core/session-manager.ts`, projection/cursors in `src/core/session-evidence.ts`, adapter selection in `src/adapters`, and contracts in `src/domain`. `scripts/agent-eval.mjs` grades reviewed repair records; it is not currently a model runner. `test/plugin-skill-contract.test.ts` includes both skill structure and deterministic qualification validation.

## Plan of Work

1. Narrow skill invocation and permission wording, move conditional connection guidance to a reference, and add a real authoring example backed by the existing native fixture.
2. Extend the evaluation contract with task categories, experiment arms, repeat identifiers, source/skill provenance, evidence references, and metrics that expose unnecessary pauses or false success.
3. Pass selected surfaces into collection. Keep verification representatives full; track cursor knowledge per collected surface and do not label omitted evidence absent.
4. Use supported Zod/SDK schema reuse where possible; maintain concrete input/output constraints and prove reference resolution with the installed client.
5. Validate the changed contracts and exercise bounded independent agent cases and native live smoke paths. Update canonical docs and record measured limits.

## Concrete Steps

Working directory: `/Users/marlonjd/Developer/monorepos/web-debug-mcp`.

- Read-only research runs concurrently; repository edits are serialized.
- Run relevant `npx vitest run <test-files>`, then `npm test`, `npm run typecheck`, `npm run build`, and `npm run harness:check`.
- Run each changed skill through the Skill Creator `quick_validate.py` and validate local links.
- Measure catalog bytes through an in-memory client without starting a browser or claiming task binding.
- Use isolated temporary directories for agent runs and command-owned browser fixtures; record commands, outcomes, model identity, and cleanup.

## Validation and Acceptance

- Source-review and exact unit-error cases avoid unnecessary browser work; explicit live Web Debug cases require its real binding.
- Prior-turn user authorization is preserved; candidate preparation continues within scope without self-approving qualification.
- Ambiguous WebMCP mutation is attempted once and remains inconclusive until independent state evidence resolves it.
- Unrequested runtime/enrichment collectors are not invoked; collected values retain privacy bounds, freshness, and truthful delta comparisons.
- All 13 tools retain valid concrete schemas and structured result validation, with a measured smaller catalog.
- Model evaluation records identify their arm/repeat and evidence; incomplete or unavailable experiments cannot become successful suites.
- Required repository checks pass and owned processes are gone. Current HMAC certification and production/release authority remain separate.

## Idempotence and Recovery

Keep all work on the current branch. Native tests own disposable temporary roots and processes. Never replay an uncertain mutation, reset user browser profiles, or modify unrelated task/plugin configuration. Resume from checked milestones and exact evidence records after context loss.

- Focused gate: 9 files / 108 tests passed. Full deterministic suite: 33 files / 190 tests passed. Typecheck and build passed; native harness 621 checks passed with certification still stale-candidate.
- All three skills passed Skill Creator quick validation in a disposable PyYAML 6.0.2 environment (system and bundled Python lacked PyYAML).
- Live Chrome WebMCP, React/Vite, Vue/Vite, and Angular smokes passed with new collection-state assertions and zero owned processes left. Next targeted-metadata rerun passed. Fresh Safari live smoke was blocked by an aborted WebDriver operation; no successful current Safari browser run is claimed. Its native fallback/freshness tests pass.

## Artifacts and Notes

Durable R0 native validation: [`../evidence/astra-native-validation-2026-09-08.json`](../evidence/astra-native-validation-2026-09-08.json). Model comparison inputs were frozen after the 190-test gate. Common comparison hashing confirmed all 38 compiled runtime files and all 10 skill files matched every workstream's frozen copy. The final R1 observation-enum correction changes one compiled runtime file and no skill files; its [separate validation](../evidence/astra-native-r1-validation-2026-09-08.json) passed 195 tests, typecheck, build, 621 harness checks, the exact Chrome regression, and React/Vite with zero owned processes remaining. The [evaluation report](../../demos/astra-skill-evaluation.md) distinguishes these scopes and the earlier high cohort.

Main native logs and catalog measurement: `/var/folders/nv/m6v_mydj3s38m58q4xrp4k1r0000gn/T/web-debug-astra-main-verification-usj3zylq`. Final routing/review artifacts: `/Users/marlonjd/Developer/monorepos/.astra-eval-routing-settings-qjjiisko`. Raw transcripts stay local; the evaluation report links the durable summaries, phase ordering, counterexamples, and final process audit.

Baseline commit: `5ae2dcd` (`docs: record 0.8.0 publication evidence`). Source was clean at task start. The existing release plan remains separate.

## Interfaces and Dependencies

Use installed MCP SDK 1.30.0, Zod 4.4.3, Playwright Core 1.62.1, and Vitest. Avoid new dependencies and extra public tools. Any capture wire change updates types, schemas, tests, and product documentation together.

## Revision History

- (2026-09-08) Created after the user authorized the complete review recommendations and parallel execution.
- (2026-09-08) Completed R1 validation, protected long-budget Astra/Luna comparisons, independent quality reviews and command audit; preserved original cohorts and recorded scoped follow-ups.
- (2026-09-08) User authorized commit and push. Combined main with the pre-existing Antigravity packaging commit passed 198 tests/34 files, typecheck, build, and 621 harness checks; [pre-push evidence](../evidence/astra-pre-push-validation-2026-09-08.json) preserves the distinction from the 195-test R1 evaluation gate.
